// Created by Credix - Licence: https://creativecommons.org/licenses/by-nc-sa/4.0/ 
#ifndef SKINCHANGER_H
#define SKINCHANGER_H

#include "../SDK/SDK.h"

namespace Skinchanger {
	void Update() {
		/*for (int i = 0; i < g_pClientEntityList->GetHighestEntityIndex(); i++) {
			CBaseCombatWeapon* Entity = (CBaseCombatWeapon*)g_pClientEntityList->GetClientEntity(i);
			if (Entity) {
				printf("%i\n", Entity->m_iWorldModelIndex);
				printf("%i\n", Entity->m_iViewModelIndex);
			}
		}*/
	}
}

#endif